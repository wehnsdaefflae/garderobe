# Garderobe Digital Platform Configuration

# Server Configuration
PORT=3000
NODE_ENV=production

# Domain for Caddy (production only, without https://)
# Example: garderobe.example.com
DOMAIN=

# Base URL Configuration
# Set this to the full URL where your application is accessible
# Must match DOMAIN above with https://
# Examples:
#   https://garderobe.example.com
#   https://coatcheck.myevent.org
# If not set, the application will auto-detect based on incoming requests
BASE_URL=

# Redis Configuration
REDIS_URL=redis://redis:6379

# Session Configuration
# Generate a random secret with: openssl rand -base64 32
SESSION_SECRET=your_cryptographically_random_secret_here

# Platform Limits
MAX_ACTIVE_EVENTS=1000              # Maximum total active events on platform
MAX_EVENTS_PER_HOUR_GLOBAL=100      # Maximum events created per hour (platform-wide)
MAX_EVENTS_PER_IP_PER_HOUR=10       # Maximum events per IP per hour
MAX_TICKETS_PER_EVENT=1000          # Maximum tickets per event

# Node Environment (development or production)
NODE_ENV=production
