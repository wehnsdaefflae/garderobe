<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="theme-color" content="#0f172a">
  <link rel="manifest" href="/manifest.json">
  <link rel="apple-touch-icon" href="/icon-192.png">
  <title>Ticket #<%= ticketId %> - Garderobe Digital</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
      background: #f8fafc;
      color: #0f172a;
      min-height: 100vh;
      padding: 20px;
      line-height: 1.5;
    }

    .container {
      max-width: 500px;
      margin: 0 auto;
    }

    .header {
      text-align: center;
      margin-bottom: 24px;
    }

    .brand {
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #64748b;
      margin-bottom: 16px;
    }

    .ticket-label {
      font-size: 14px;
      font-weight: 500;
      color: #64748b;
      margin-bottom: 8px;
    }

    .ticket-number {
      font-size: 96px;
      font-weight: 900;
      line-height: 1;
      color: #0f172a;
      letter-spacing: -4px;
    }

    .qr-container {
      background: #ffffff;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      padding: 32px;
      margin: 32px 0;
      text-align: center;
      box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    }

    .qr-code {
      width: 100%;
      max-width: 280px;
      height: auto;
      display: block;
      margin: 0 auto;
    }

    .warning-box {
      background: #ffffff;
      border: 1px solid #fbbf24;
      border-left: 4px solid #fbbf24;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 16px;
    }

    .warning-title {
      font-size: 16px;
      font-weight: 600;
      margin-bottom: 12px;
      color: #0f172a;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .warning-icon {
      width: 20px;
      height: 20px;
      color: #fbbf24;
    }

    .warning-list {
      list-style: none;
      padding: 0;
    }

    .warning-list li {
      font-size: 14px;
      color: #475569;
      margin-bottom: 8px;
      padding-left: 28px;
      position: relative;
      line-height: 1.6;
    }

    .warning-list li::before {
      content: "";
      position: absolute;
      left: 8px;
      top: 8px;
      width: 6px;
      height: 6px;
      background: #fbbf24;
      border-radius: 50%;
    }

    .action-button {
      width: 100%;
      padding: 14px;
      background: #0d9488;
      border: none;
      border-radius: 6px;
      color: #ffffff;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: background 150ms ease-in-out;
      box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      font-family: inherit;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      margin-bottom: 12px;
    }

    .action-button:hover {
      background: #0f766e;
    }

    .action-button:active {
      background: #115e59;
    }

    .action-icon {
      width: 20px;
      height: 20px;
    }

    .info-box {
      background: #ecfdf5;
      border: 1px solid #a7f3d0;
      border-radius: 8px;
      padding: 16px;
      margin-top: 20px;
      font-size: 13px;
      line-height: 1.6;
      color: #064e3b;
    }

    .info-title {
      font-weight: 600;
      margin-bottom: 8px;
      color: #065f46;
    }

    .save-methods {
      list-style: none;
      padding: 0;
      margin-top: 8px;
    }

    .save-methods li {
      font-size: 13px;
      color: #065f46;
      margin-bottom: 4px;
      padding-left: 20px;
      position: relative;
    }

    .save-methods li::before {
      content: "•";
      position: absolute;
      left: 8px;
      font-weight: bold;
    }

    @media (max-width: 480px) {
      .ticket-number {
        font-size: 72px;
      }

      .qr-container {
        padding: 24px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="brand">Garderobe Digital</div>
      <div class="ticket-label">Your Ticket Number</div>
      <div class="ticket-number">#<%= ticketId %></div>
    </div>

    <div class="qr-container">
      <img src="/e/<%= slug %>/api/qr/<%= ticketId %>?token=<%= token %>" alt="QR Code" class="qr-code" id="qrCode">
    </div>

    <div class="warning-box">
      <div class="warning-title">
        <svg class="warning-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
        </svg>
        Important - Save This Page
      </div>
      <ul class="warning-list">
        <li>This is your only way to retrieve your coat</li>
        <li>Do not close this page or your browser</li>
        <li>Do not share your screen with anyone</li>
        <li>You can find it in your browser history if needed</li>
      </ul>
    </div>

    <button class="action-button" id="saveQrBtn">
      <svg class="action-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
      </svg>
      Save QR Code Image
    </button>

    <div class="info-box">
      <div class="info-title">Ways to save your ticket:</div>
      <ul class="save-methods">
        <li>Take a screenshot of this page</li>
        <li>Tap the button above to download the QR code</li>
        <li>Keep this browser tab open until you retrieve your coat</li>
      </ul>
    </div>
  </div>

  <script>
    // Prevent accidental navigation away
    window.addEventListener('beforeunload', function (e) {
      e.preventDefault();
      e.returnValue = '';
    });

    // Save QR Code as image
    document.getElementById('saveQrBtn').addEventListener('click', async function() {
      try {
        const qrImg = document.getElementById('qrCode');
        const response = await fetch(qrImg.src);
        const blob = await response.blob();
        const url = window.URL.createObjectURL(blob);

        const a = document.createElement('a');
        a.style.display = 'none';
        a.href = url;
        a.download = 'garderobe-ticket-<%= ticketId %>.png';
        document.body.appendChild(a);
        a.click();
        window.URL.revokeObjectURL(url);
        document.body.removeChild(a);

        // Visual feedback
        this.innerHTML = '<svg class="action-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>Saved to Downloads!';
        setTimeout(() => {
          this.innerHTML = '<svg class="action-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>Save QR Code Image';
        }, 3000);
      } catch (error) {
        console.error('Error saving QR code:', error);
        alert('Could not download QR code. Try taking a screenshot instead!');
      }
    });

    // Register service worker for offline support
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.register('/sw.js')
        .then((registration) => {
          console.log('ServiceWorker registered:', registration);
        })
        .catch((error) => {
          console.log('ServiceWorker registration failed:', error);
        });
    }
  </script>
</body>
</html>
